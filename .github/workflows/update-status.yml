name: Update Bot Status

on:
  schedule:
    - cron: '*/15 * * * *' # Runs every 15 minutes
  workflow_dispatch: # Allows manual trigger

jobs:
  update-status:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create data directory
        run: mkdir -p data

      - name: Fetch WhatsApp (Koyeb) Status
        run: |
          STATUS_JSON=$(curl -s "https://alexaincsl-alexa.hf.space/status")
          PHONE_JSON=$(curl -s "https://alexaincsl-alexa.hf.space/get-phone-number")
          
          # Use jq to extract values or set defaults
          WA_STATUS=$(echo $STATUS_JSON | jq -r '.status // "OFFLINE"')
          WA_PHONE=$(echo $PHONE_JSON | jq -r '.phoneNumber // "94705327164"')
          
          echo "WA_STATUS=$WA_STATUS" >> $GITHUB_ENV
          echo "WA_PHONE=$WA_PHONE" >> $GITHUB_ENV

      - name: Fetch AlexaTG Status
        run: |
          RESPONSE=$(curl -s "https://alexatgsl-alexatg.hf.space/" \
            -H "accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7" \
            -H "accept-language: en-GB,en-US;q=0.9,en;q=0.8" \
            -H "cache-control: max-age=0" \
            -H "priority: u=0, i" \
            -H 'sec-ch-ua: "Not=A?Brand";v="24", "Chromium";v="140"' \
            -H "sec-ch-ua-mobile: ?0" \
            -H 'sec-ch-ua-platform: "Windows"' \
            -H "sec-fetch-dest: document" \
            -H "sec-fetch-mode: navigate" \
            -H "sec-fetch-site: none" \
            -H "sec-fetch-user: ?1" \
            -H "upgrade-insecure-requests: 1")
          
          if echo "$RESPONSE" | grep -qi "alive"; then
            STATUS="ONLINE"
            UPDATE="System: Active & Responsive"
          else
            STATUS="OFFLINE"
            UPDATE="System: Offline / Starting"
          fi
          echo "TG_STATUS=$STATUS" >> $GITHUB_ENV
          echo "TG_UPDATE=$UPDATE" >> $GITHUB_ENV

      - name: Fetch Snowstrom Status
        run: |
          RESPONSE=$(curl -s "https://alexasnowst-snowstrom.hf.space/" \
            -H "accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7" \
            -H "accept-language: en-GB,en-US;q=0.9,en;q=0.8" \
            -H "cache-control: max-age=0" \
            -H "priority: u=0, i" \
            -H 'sec-ch-ua: "Not=A?Brand";v="24", "Chromium";v="140"' \
            -H "sec-ch-ua-mobile: ?0" \
            -H 'sec-ch-ua-platform: "Windows"' \
            -H "sec-fetch-dest: document" \
            -H "sec-fetch-mode: navigate" \
            -H "sec-fetch-site: none" \
            -H "sec-fetch-user: ?1" \
            -H "upgrade-insecure-requests: 1")
          
          if echo "$RESPONSE" | grep -qi "alive"; then
            STATUS="ONLINE"
            UPDATE="System: Active & Responsive"
          else
            STATUS="OFFLINE"
            UPDATE="System: Offline / Starting"
          fi
          echo "SNOW_STATUS=$STATUS" >> $GITHUB_ENV
          echo "SNOW_UPDATE=$UPDATE" >> $GITHUB_ENV

      - name: Fetch Alexa Music Status
        run: |
          RESPONSE=$(curl -s "https://alexaformusic-alexamusic.hf.space/" \
            -H "accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7" \
            -H "accept-language: en-GB,en-US;q=0.9,en;q=0.8" \
            -H "cache-control: max-age=0" \
            -H "priority: u=0, i" \
            -H 'sec-ch-ua: "Not=A?Brand";v="24", "Chromium";v="140"' \
            -H "sec-ch-ua-mobile: ?0" \
            -H 'sec-ch-ua-platform: "Windows"' \
            -H "sec-fetch-dest: document" \
            -H "sec-fetch-mode: navigate" \
            -H "sec-fetch-site: none" \
            -H "sec-fetch-user: ?1" \
            -H "upgrade-insecure-requests: 1")
          
          if echo "$RESPONSE" | grep -qi "alive"; then
            STATUS="ONLINE"
            UPDATE="System: Active & Responsive"
          else
            STATUS="OFFLINE"
            UPDATE="System: Offline / Starting"
          fi
          
          echo "MUSIC_STATUS=$STATUS" >> $GITHUB_ENV
          echo "MUSIC_UPDATE=$UPDATE" >> $GITHUB_ENV

      - name: Keep Quote API Alive
        run: |
          RESPONSE=$(curl -s "https://huggingface.co/spaces/quotlytga/quoteapi" \
            -H "accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7" \
            -H "accept-language: en-GB,en-US;q=0.9,en;q=0.8" \
            -H "cache-control: max-age=0" \
            -H "priority: u=0, i" \
            -H 'sec-ch-ua: "Not=A?Brand";v="24", "Chromium";v="140"' \
            -H "sec-ch-ua-mobile: ?0" \
            -H 'sec-ch-ua-platform: "Windows"' \
            -H "sec-fetch-dest: document" \
            -H "sec-fetch-mode: navigate" \
            -H "sec-fetch-site: none" \
            -H "sec-fetch-user: ?1" \
            -H "upgrade-insecure-requests: 1")
          
          if echo "$RESPONSE" | grep -qi "Hugging Face\|Quote"; then
            STATUS="ONLINE"
            UPDATE="System: Active & Responsive"
          else
            STATUS="OFFLINE"
            UPDATE="System: Offline / Starting"
          fi
          
          echo "QUOTE_STATUS=$STATUS" >> $GITHUB_ENV
          echo "QUOTE_UPDATE=$UPDATE" >> $GITHUB_ENV

      - name: Keep Anarkani Music Alive
        run: |
          RESPONSE=$(curl -s "https://huggingface.co/spaces/anarkanimusic/anarkanimuisic" \
            -H "accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7" \
            -H "accept-language: en-GB,en-US;q=0.9,en;q=0.8" \
            -H "cache-control: max-age=0" \
            -H "priority: u=0, i" \
            -H 'sec-ch-ua: "Not=A?Brand";v="24", "Chromium";v="140"' \
            -H "sec-ch-ua-mobile: ?0" \
            -H 'sec-ch-ua-platform: "Windows"' \
            -H "sec-fetch-dest: document" \
            -H "sec-fetch-mode: navigate" \
            -H "sec-fetch-site: none" \
            -H "sec-fetch-user: ?1" \
            -H "upgrade-insecure-requests: 1")
          
          if echo "$RESPONSE" | grep -qi "Hugging Face\|Music"; then
            STATUS="ONLINE"
            UPDATE="System: Active & Responsive"
          else
            STATUS="OFFLINE"
            UPDATE="System: Offline / Starting"
          fi
          
          echo "ANARKANI_STATUS=$STATUS" >> $GITHUB_ENV
          echo "ANARKANI_UPDATE=$UPDATE" >> $GITHUB_ENV


      - name: Generate status.json
        run: |
          cat <<EOF > data/status.json
          {
            "alexa-v3": {
              "status": "${{ env.WA_STATUS }}",
              "update": "System: Active & Responsive",
              "phoneNumber": "${{ env.WA_PHONE }}"
            },
            "alexatg": {
              "status": "${{ env.TG_STATUS }}",
              "update": "${{ env.TG_UPDATE }}"
            },
            "alexaxmusic": {
              "status": "${{ env.MUSIC_STATUS }}",
              "update": "${{ env.MUSIC_UPDATE }}"
            },
            "snowstrom": {
              "status": "${{ env.SNOW_STATUS }}",
              "update": "${{ env.SNOW_UPDATE }}"
            },
            "quoteapi": {
              "status": "${{ env.QUOTE_STATUS }}",
              "update": "${{ env.QUOTE_UPDATE }}"
            },
            "anarkanimusic": {
              "status": "${{ env.ANARKANI_STATUS }}",
              "update": "${{ env.ANARKANI_UPDATE }}"
            },
            "last_check": "$(date -u)"
          }
          EOF



      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add data/status.json
          git commit -m "chore: update bot status [skip ci]" || echo "No changes to commit"
          git push
